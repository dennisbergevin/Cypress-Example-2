{
  "name": "cypress-example",
  "version": "1.0.0",
  "description": "Cypress Example",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --e2e --browser electron",
    "open": "npx cypress open --e2e --browser electron"
  },
  "devDependencies": {
    "@testing-library/cypress": "^8.0.2",
    "cypress": "^10.3.0"
  }
}
